GATEWAY_JWT_SECRET=
REDIS_PASSWORD=changeme

# Database Type: docker (local PG), remote (external PG), sqlite
DB_TYPE=docker
DB_HOST=localhost
DB_PORT=5432
DB_NAME=aigateway
DB_USER=postgres
DB_PASSWORD=postgres123

# OTLP trace export endpoint (gRPC).  Leave empty to disable.
OTLP_ENDPOINT=http://jaeger:4317
